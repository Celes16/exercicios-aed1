#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
bool canPlaceFlowers(int* flowerbed, int flowerbedSize, int n) {
    int* flowerbed = malloc (*int) flowebedSize* sizeof(int);

    for (i=0; i < flowerbedSize.length; i++) {
        for ( n = 1 || n = 0) {

        if flowerbed[i] = 1 {
        printf ("planta uma flor: %d", n ); 
        return true;
        }
        if flowerbed[i] = 0 {
        printf ("sem flor: %d", n ); 
        return false;
        }
    }
    
    free (*flowebedSize);
    return 0;
}

int main () {
    int* flowerbed = [0,1,0,1,0];
    int n = 1

}